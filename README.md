# Simple Node rest api service

# Run intructions

 - Open the project folder in linux terminal and type for install node dependecies : npm install
 - Start the Api rest in localhost:3000 using : node index.js 
